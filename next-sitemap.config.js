module.exports = {
    siteUrl: 'https://volety.dev', // Your website URL
    generateRobotsTxt: true, // Generate a robots.txt file
    // The default sitemap size (5000 URLs per sitemap file) is used here
    outDir: './public', // Output directory for the sitemap files is set to 'public'
    // No specific paths are excluded since you indicated that all pages should be indexed
    // No additional sitemaps or URL transformations are needed as per your responses
  };
  